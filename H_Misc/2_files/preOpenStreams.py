#input() function reads data from sys.stdin by default.
# print() function outputs the data to the sys.stdout stream.
#send information on the errors encountered(sys.stderr)
'''these are already opened by default'''
#in js we generally use them